//T// assignment operators: =, +=, -=, *=, /=, %=, &=, |=, ^=, <<=, >>=
    C = A + B;
    C += A;
    C -= A;
    C *= A;
    C /= A;
    C %= A;
    b3 &= b1;
    b3 |= b1;
    b3 ^= b1;
    b3 <<= 2;
    b3 >>= 2;

//T// ternary operator: ?, :
    A = p ? 12 : -12;